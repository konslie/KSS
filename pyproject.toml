[project]
name = "kss"
version = "0.1.0"
description = "Korean stock quantitative valuation engine"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
kss = "kss.cli:main"

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
