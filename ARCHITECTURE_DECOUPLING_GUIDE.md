# KSS 데이터-프론트엔드 디커플링 아키텍처 가이드 (Architecture Decoupling Guide)

본 가이드는 아침 투자 브리핑(KSS) 파이프라인의 데이터 수집, 가공, 렌더링 과정을 완전히 디커플링(Decoupling)하여 유연하고 확장성 있는 모던 웹 아키텍처로 개선하기 위한 설계 및 개발 가이드입니다. 

---

## 1. 현재 아키텍처의 문제점 및 한계

현재 프로젝트는 마크다운 파서와 프론트엔드가 강하게 결합(Tightly Coupled)되어 있어 다음과 같은 한계가 존재합니다.

* **데이터 중복 및 수동 작성**: 지표 및 포트폴리오 데이터가 `view_model.json`에 이미 존재함에도 불구하고, 에이전트(LLM)나 사용자가 `final.md` 내에 마크다운 표 구조를 똑같이 다시 작성해야 합니다.
* **백엔드의 화면 제어**: 파이썬 파서([render_html.py](file:///Users/konslie/Desktop/Codex/KSS/src/render_html.py))가 텍스트 내에 줄바꿈 기호(`<br>`), CSS 클래스명 등을 강제로 주입하여 데이터 형식을 변경합니다.
* **프론트엔드의 비정상적인 가로채기(Intercept)**: 프론트엔드 자바스크립트가 특정 마크다운 섹션 이름(`주요 거시지표`, `포트폴리오 영향도`)을 하드코딩된 문자열로 감지하여 렌더링을 건너뛰고, 그 자리에 JSON 데이터로 만든 대시보드를 끼워 넣는 기형적인 구조를 취하고 있습니다.
* **에셋 관리 불편**: CSS와 JS 코드가 파이썬 파일 내에 거대한 문자열 상수로 저장되어 있어, UI 디자인 수정 시 문법 강조(Syntax Highlighting) 및 린터 등의 개발 도구 지원을 받을 수 없습니다.

---

## 2. 지향하는 모던 디커플링 아키텍처

역할을 철저히 분리하여 **데이터(Data), 마크다운 본문(Prose), 화면 프레젠테이션(UI)**이 각각 독립적으로 동작하게 만듭니다.

```
┌──────────────────┐      ┌──────────────────┐
│  view_model.json │      │     final.md     │
│  (순수 수치 데이터) │      │  (서면 코멘트만)   │
└────────┬─────────┘      └────────┬─────────┘
         │                         │
         │ (JSON 데이터 주입)         │ (단순 HTML 파싱 변환)
         ▼                         ▼
   ┌─────────────────────────────────────────┐
   │            Python HTML 빌더             │
   │      (마크다운 표 조작 없음, 템플릿 결합)      │
   └───────────────────┬─────────────────────┘
                       │
                       ▼ (index.html / YYYY-MM-DD.html 생성)
   ┌─────────────────────────────────────────┐
   │         CSS/JS 기반 독립 프론트엔드         │
   │  - JSON 데이터를 바탕으로 표 동적 렌더링       │
   │  - 마크다운 본문은 아래 영역에 그대로 출력      │
   └─────────────────────────────────────────┘
```

---

## 3. 세부 설계 및 데이터 명세

### 3.1. 마크다운 본문 (`final.md`) 가이드라인
더 이상 마크다운 본문 내에 복잡한 표(주요 거시지표, 포트폴리오 영향도)를 작성하지 않습니다. 에이전트나 사용자는 **순수 텍스트 형태의 리포트 섹션만 마크다운으로 작성**합니다.
```markdown
# Morning Investment Briefing - YYYY-MM-DD

## 1. Executive Summary
- **오늘의 한줄 요약:** 시장 급락 속 금융지주사 강세 및 반도체 조정.
- **시장 위험도:** 주의

## 3. 금융주 브리핑
금융지주사들은 금리 인하 기대감 축소와 고배당 매력으로 강세를 보였습니다...

## 4. 현대차 / 환율
원/달러 환율이 1,540원을 터치하며 수출 기업의 마진 우려가 깊어졌으나...
```

### 3.2. 정제된 뷰 모델 데이터 (`view_model.json`)
표현(Presentation) 태그를 배제하고 오직 가공되지 않은 **정량적 숫자와 원시 데이터**만 보존합니다.
```json
{
  "date": "2026-06-05",
  "market_indicators": [
    {
      "name": "KOSPI",
      "latest_close": 8160.59,
      "change": -478.82,
      "change_pct": -5.54,
      "recent_closes": [8228.7, 8185.3, 8476.1, 8788.3, 8801.4, 8639.4, 8160.59]
    }
  ],
  "holdings": [
    {
      "name": "하나금융지주",
      "symbol": "086790",
      "latest_close": 123600,
      "change": 3000,
      "change_pct": 2.49,
      "flow": {
        "institution": 5031234550,
        "foreign": 8832039100
      },
      "rationale": "시장 급락 속 금융지주 강세 메커니즘 작동."
    }
  ]
}
```

---

## 4. 프론트엔드 UI/UX 설계 및 렌더링 규칙

자바스크립트([report.js](file:///Users/konslie/Desktop/Codex/KSS/docs/assets/report.js))와 CSS([report.css](file:///Users/konslie/Desktop/Codex/KSS/docs/assets/report.css))가 화면 렌더링을 완전히 통제합니다.

### 4.1. 테이블 및 대시보드 동적 생성 (JS 역할)
* **시장 지표**: `view_model.market_indicators` 데이터를 순회하며, DOM API를 이용해 지표별 카드를 동적으로 렌더링하고 최근 closes 데이터를 기반으로 `SVG sparkline`을 생성합니다.
* **포트폴리오 현황**: `view_model.holdings`를 루프 돌려 수급 수치(양수/음수)에 따라 알맞은 스타일 클래스(`value-up`/`value-down`)를 자동으로 붙이고 표(`<table>`)를 그립니다.
* **자동 볼드 키워드 처리**: 하드코딩 리스트 대신 `view_model.holdings`에 등록된 실제 종목명 데이터를 가져와 본문 텍스트 내에서 동적으로 매칭하여 볼드 효과를 부여합니다.

### 4.2. 일관된 스타일링 통제 (CSS 역할)
* 반응형 레이아웃(PC/태블릿/모바일)과 다크모드/라이트모드 스타일링은 오직 CSS 커스텀 속성(CSS Variables)으로만 통제합니다.
* 파이썬 백엔드 빌더로부터 인라인 스타일이나 레이아웃용 태그를 주입받지 않습니다.

---

## 5. 단계별 리팩토링 구현 로드맵

새로운 아키텍처를 적용하기 위한 구체적인 개발 단계입니다.

### [1단계] 프론트엔드 에셋 독립 분리
* `render_html.py` 내부의 거대한 파이썬 문자열 상수 `REPORT_CSS`, `REPORT_JS`를 제거합니다.
* 독립된 에셋 파일인 `docs/assets/report.css`와 `docs/assets/report.js`를 직접 편집하도록 배치를 수정하고, 파이썬 스크립트는 템플릿 컴파일 시 외부 파일을 읽어와 빌드하도록 수정합니다.

### [2단계] 파이썬 렌더러 기능 다이어트
* `render_html.py` 내의 `normalize_portfolio_rows`, `split_flow`, `table_class`, `auto_bold_section` 등 **화면 레이아웃에 관여하는 파이썬 정규식 함수들을 전부 삭제**합니다.
* 마크다운 본문 파싱은 단순히 문단 분리 및 기본 마크다운 태그 변환만 수행하고(혹은 표준 라이브러리 활용), 템플릿 본문 변환 본을 `report_data["elements"]`에 담는 일로 한정합니다.

### [3단계] 자바스크립트 렌더러 전면 개편
* [report.js](file:///Users/konslie/Desktop/Codex/KSS/docs/assets/report.js) 내부의 렌더링 흐름을 수정합니다.
* `readReportData` 함수를 통해 순수 `view_model` 객체를 꺼낸 다음, `renderMarketCards()`, `renderHoldingMatrix()` 등의 함수를 호출하여 화면 상단 영역에 대시보드를 생성합니다.
* 마크다운 본문 내용(`report.elements`)은 하단 영역에 가로채기(Intercept) 없이 있는 그대로 렌더링되게 만듭니다.

### [4단계] 데이터 포맷 유효성 검사 및 테스트
* 수정된 `view_model.json` 스키마가 프론트엔드 렌더링에 적합한지 검사하는 유닛 테스트를 추가하고, 데이터 수집 에이전트(Codex)의 지침서(Prompt)를 갱신하여 더 이상 마크다운 본문 내부에 중복 표를 작성하지 않도록 규칙을 보완합니다.
